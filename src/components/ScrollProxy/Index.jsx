// ScrollProxy is no longer needed — Locomotive Scroll has been removed.
// This file is kept as a no-op stub to avoid any stray import errors.
const ScrollProxy = () => null;
export default ScrollProxy;
